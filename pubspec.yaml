name: eva_generic_db_service
description: Generic PostgreSQL CRUD service for EVA.
version: 1.0.0

environment:
  sdk: ^3.12.2

dependencies:
  eva_sdk: ^0.4.0
  postgres: ^3.5.6
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
